# unity.git

## howto

- eg: Save the current working directory as a git bundle

  ```bash
  nu ./unity.git.nu
  ```

  ```powershell
  unity.git.bat
  ```

- eg: Unpack a git bundle

  ```powershell
  git clone E:/backup/my-unity-projek_2026-05-22-170505.bundle C:/dev/unity/my-unity-projek
  ```

